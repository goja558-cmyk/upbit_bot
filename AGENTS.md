# 작업 가이드라인

이 저장소를 수정하는 모든 봇/모델은 아래 규칙을 따른다.

## 기본 원칙

- 작업 전에 `git status`와 README를 확인하고, 기존 변경사항을 덮어쓰지 않는다.
- 요청과 직접 관련된 최소 파일만 수정한다. 근거 없는 전략 변경이나 대규모 리팩터링은 금지한다.
- API 키, 계좌번호, 텔레그램 토큰·chat ID 및 운영 설정을 코드·로그·커밋에 넣지 않는다.
- 운영 상태(`shared/`, `data/`, `logs/`)와 개인 설정은 저장소에 커밋하지 않는다.
- 커밋 메시지에는 변경 목적과 주문/알림 동작에 미치는 영향을 적는다.

## 금융 안전 원칙

- 실거래 주문 로직을 바꿀 때는 주문 조건, 수량, 중복 주문 방지, 장중/장외 시간 경계를 함께 검토한다.
- 백테스트·관찰 코드와 실거래 코드를 혼동하지 않는다. 백테스트가 실거래 주문을 호출해서는 안 된다.
- API 응답 누락·지연·오류를 정상 데이터로 처리하지 말고, 안전하게 중단하거나 건너뛴다.
- 운영 프로세스와 서비스는 사용자의 명시적 지시 없이 시작·중지·재시작하지 않는다.

## 검증

- Python 변경 후 `python -m py_compile <변경 파일>`을 실행한다.
- 가능하면 주문 없는 테스트/백테스트로 먼저 확인하고, 실제 주문 API는 호출하지 않는다.
- 변경된 실행 경로와 README의 실행 예시가 일치하는지 확인한다.
- 검증 결과와 남은 위험을 작업 결과에 기록한다.
